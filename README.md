Для запуска приложения требуется установленные Docker и Docker Compose.

1. Собрать докер образ с приложением `docker build -f docker/Dockerfile -t socnetwork .`
2. Запустить Docker Compose `docker compose up -d`

Приложение слушает дефолтный порт 8080.

